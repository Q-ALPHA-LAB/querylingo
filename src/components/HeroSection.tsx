import React from 'react';

const HeroSection: React.FC = () => {
  return (
    <section className="hero-section">
      <div className="container">
        <h2>Transform English into SQL & MongoDB Queries</h2>
        <p>AI-DB is a revolutionary database service that converts natural language queries into SQL and MongoDB commands. Empower your data with intuitive search.</p>
        <button>Get Started</button>
      </div>
    </section>
  );
};

export default HeroSection;
